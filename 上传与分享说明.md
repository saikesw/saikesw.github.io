# FIELDLAB 上传与分享说明

## 一、上传到 GitHub Pages

### 必须上传的网站文件

```text
index.html
css/
  styles.css
js/
  physics.js
  field-renderer.js
  surface-renderer.js
  app.js
```

发布包里还包含：

- `.nojekyll`：让 GitHub Pages 直接按静态文件发布。
- `README.md`：项目、模型和测试说明。
- `上传与分享说明.md`：本说明文件。

后面三个文件不会影响网站运行，可以一并上传。

### GitHub 网页端操作

1. 登录 GitHub，点击右上角 `+`，选择 `New repository`。
2. 仓库名可填 `fieldlab`，选择 `Public`，创建仓库。
3. 打开本文件所在的 `FIELDLAB-GitHub-Pages` 文件夹。
4. 在 GitHub 仓库中选择 `Add file` → `Upload files`。
5. 上传 **该文件夹里面的全部内容**，不要只上传外层文件夹。上传后，仓库根目录必须能直接看到 `index.html`。
6. 提交文件后，进入仓库 `Settings` → `Pages`。
7. 在 `Build and deployment` 中，将 Source 设为 `Deploy from a branch`。
8. Branch 选择 `main`，目录选择 `/ (root)`，点击 `Save`。
9. 等待 GitHub 构建完成。一般访问地址为：

```text
https://你的GitHub用户名.github.io/fieldlab/
```

如果仓库名恰好是 `你的GitHub用户名.github.io`，地址则为：

```text
https://你的GitHub用户名.github.io/
```

### 上传后检查

- 仓库根目录有 `index.html`，而不是 `FIELDLAB-GitHub-Pages/index.html`。
- `css` 与 `js` 文件夹结构不能打散。
- 文件名大小写保持不变。
- Pages 设置使用 `main` 分支和 `/ (root)`。

## 二、发给朋友

最简单的方式是把同目录下的 `FIELDLAB-GitHub-Pages.zip` 发给朋友。

朋友收到后：

1. 先完整解压 ZIP，不要直接在压缩包预览窗口里打开。
2. 保持 `index.html`、`css`、`js` 的目录结构不变。
3. 双击解压目录中的 `index.html`，即可在浏览器中运行。

朋友实际运行网站所需的最少内容只有：

```text
index.html
css/styles.css
js/physics.js
js/field-renderer.js
js/surface-renderer.js
js/app.js
```

也可以直接把 GitHub Pages 网站链接发给朋友，这样对方不需要接收任何文件。
